version: '3.8'

services:
  mongodb:
    image: mongo:7.0
    container_name: vedaai-mongo
    restart: unless-stopped
    ports:
      - '27017:27017'
    environment:
      MONGO_INITDB_ROOT_USERNAME: vedaai
      MONGO_INITDB_ROOT_PASSWORD: vedaai_secret
      MONGO_INITDB_DATABASE: vedaai
    volumes:
      - mongo_data:/data/db

  redis:
    image: redis:7.2-alpine
    container_name: vedaai-redis
    restart: unless-stopped
    ports:
      - '6379:6379'
    command: redis-server --appendonly yes
    volumes:
      - redis_data:/data

volumes:
  mongo_data:
  redis_data:
